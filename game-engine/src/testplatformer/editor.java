package testplatformer;

import engine.scene;
import engine.window;

public class editor extends scene {

    public editor(window Parrent, String id) {
        super(Parrent, id);
        // addEntity(e, name);
    }

    
    
}